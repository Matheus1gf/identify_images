# pdf_identify
Inteligencia artificial em cima da geração dos pdfs

Para executar o código corretamente vocês devem ter instalado na máquina de vocês o openCV, para instalar basta usar pip install opencv-python

Para executar o código basta rodar python detect_shapes.py --image decsau.png
